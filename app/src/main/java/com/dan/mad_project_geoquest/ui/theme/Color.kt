package com.dan.mad_project_geoquest.ui.theme

import androidx.compose.ui.graphics.Color

// ── Existing palette ─────────────────────────────────────────────────────────
val Parchment       = Color(0xFFF5E6C8)   // Warm off-white — light background
val Cream           = Color(0xFFFFF8EC)   // Lightest surface / onPrimary light
val Leather         = Color(0xFFC8924A)   // Terracotta — secondary light / tertiary dark
val Gold            = Color(0xFFE8A83A)   // Primary accent — dark theme
val Sand            = Color(0xFFD4A96A)   // Muted gold — secondary dark / outline
val Brown           = Color(0xFF7A5533)   // Mid brown — surface dark
val DarkBrown       = Color(0xFF3B2507)   // Darkest — background dark / ink text light

// ── New tokens for full M3 light-theme support ───────────────────────────────
val DarkGold        = Color(0xFF8B6914)   // Gold darkened for contrast on Parchment
val PaleParchment   = Color(0xFFFAF0DC)   // Lighter than Parchment — tonal containers
val MapRed          = Color(0xFF8B2020)   // Compass/error red — warm, on-brand